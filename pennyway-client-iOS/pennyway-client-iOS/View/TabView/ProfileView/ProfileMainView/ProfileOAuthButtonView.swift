
import SwiftUI

struct ProfileOAuthButtonView: View {
    var body: some View {
        VStack {
            Text("Subview 2")
        }

        .frame(maxWidth: .infinity)
        .background(Color("White01"))
    }
}
