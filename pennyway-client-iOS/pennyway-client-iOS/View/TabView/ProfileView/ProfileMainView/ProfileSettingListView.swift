

import SwiftUI

struct ProfileSettingListView: View {
    var body: some View {
        VStack {
            Text("Subview 3")
        }

        .frame(maxWidth: .infinity)
        .background(Color("White01"))
    }
}
